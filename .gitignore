.venv/
__pycache__/
*.pyc
.claude/
.DS_Store

# the interactive site lives in its own repo (GitHub Pages)
docs/

# large, regenerable MCMC outputs (rerun the pipeline to recreate)
results/draws*.npz
results/*.log

# old (buggy) roll-call cache; term10_rollcall_v2.pkl is the input snapshot
data/term10_rollcall.pkl
